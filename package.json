{
  "name": "sequence-visualiser",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@emotion/react": "*",
    "@emotion/styled": "*",
    "@mui/material": "*",
    "bootstrap": "^5.3.1",
    "plotly.js": "^2.20.0",
    "react": "^18.2.0",
    "react-bootstrap": "^2.8.0",
    "react-dom": "^18.2.0",
    "react-plotly.js": "^2.6.0",
    "react-router-dom": "^6.15.0",
    "react-scripts": "^5.0.1",
    "settings": "file:./settings.json",
    "socket.io-client": "^4"
  },
  "devDependencies": {
    "prettier": "^3.2.4"
  },
  "scripts": {
    "prestart": "cp scripts/pre-commit .git/hooks/ && chmod u+x .git/hooks/pre-commit && echo 'hook copied'",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
